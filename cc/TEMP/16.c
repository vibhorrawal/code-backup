#include<stdio.h>
int leaf_example (int g, int h, int i, int j)
{
int f;
f = (g + h) - (i + j);
return f;
}

int main(int argc, char const *argv[])
{
	int x,z,c,v;
	leaf_example(z,x,c,v);
	return 0;
}